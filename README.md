This backend is deployed to AWS, with front end deployed and reachable at sheetassistapp.com.

This backend involves an LLM writing and executing code in a sandbox environment, similar to ChatGPT's "code interpreter" tool.  However, my algorithm adds a verification layer to ensure that the output will satisfy the user's query, not just be the output of error free code.
